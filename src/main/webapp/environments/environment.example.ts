export const environment = {
  VERSION: 'DEV',
  DEBUG_INFO_ENABLED: true,
  ldClientId: 'YOUR_LAUNCHDARKLY_CLIENT_SIDE_ID_HERE', // Get from LaunchDarkly: Account settings > Projects > [project] > Environments > [env] > Client-side ID
};
